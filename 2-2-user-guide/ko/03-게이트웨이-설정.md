# 게이트웨이 설정

## 설정 파일
`~/.openclaw/openclaw.json` (JSON5 형식 - 주석 + 후행 쉼표 허용)

## 유효성 검사
- 시작 시 엄격한 스키마 검증
- 알 수 없는 키 → 게이트웨이 시작 거부
- `openclaw doctor`로 진단
- `openclaw doctor --fix`로 마이그레이션 적용

## 최소 설정

```json5
{
  agents: { defaults: { workspace: "~/.openclaw/workspace" } },
  channels: { whatsapp: { allowFrom: ["+15555550123"] } },
}
```

## 주요 설정 섹션

### 환경 변수
```json5
{
  env: {
    OPENROUTER_API_KEY: "sk-or-...",
    vars: { GROQ_API_KEY: "gsk-..." }
  }
}
```

설정 내 대체: `${VAR_NAME}` 문법 사용.

### 로깅
```json5
{
  logging: {
    level: "info",
    file: "/tmp/openclaw/openclaw.log",
    consoleLevel: "info",
    redactSensitive: "tools"
  }
}
```

### 에이전트 정체성
```json5
{
  agents: {
    list: [{
      id: "main",
      identity: {
        name: "Samantha",
        theme: "helpful sloth",
        emoji: "🦥",
        avatar: "avatars/samantha.png"
      }
    }]
  }
}
```

### 멀티 에이전트 라우팅
```json5
{
  agents: {
    list: [
      { id: "home", default: true, workspace: "~/.openclaw/workspace-home" },
      { id: "work", workspace: "~/.openclaw/workspace-work" }
    ]
  },
  bindings: [
    { agentId: "home", match: { channel: "whatsapp", accountId: "personal" } },
    { agentId: "work", match: { channel: "whatsapp", accountId: "biz" } }
  ]
}
```

### 메시지 큐
```json5
{
  messages: {
    queue: {
      mode: "collect",  // steer | followup | collect | interrupt
      debounceMs: 1000,
      cap: 20
    }
  }
}
```

### 응답 접두사
```json5
{
  messages: {
    responsePrefix: "[{model} | think:{thinkingLevel}]",
    ackReaction: "👀",
    ackReactionScope: "group-mentions"
  }
}
```

## DM 정책

| 정책 | 동작 |
|------|------|
| `pairing` | 모르는 발신자에게 페어링 코드 (기본값) |
| `allowlist` | `allowFrom` 목록만 허용 |
| `open` | 모두 허용 (`allowFrom: ["*"]` 필요) |
| `disabled` | 모두 무시 |

## 그룹 정책
- `open`: 그룹이 허용 목록 우회
- `disabled`: 모든 그룹 메시지 차단
- `allowlist`: 설정된 그룹만 허용 (기본값)

## 설정 RPC

```bash
# 현재 설정 가져오기
openclaw gateway call config.get --params '{}'

# 설정 패치 (병합 업데이트)
openclaw gateway call config.patch --params '{
  "raw": "{ channels: { telegram: { groups: { \"*\": { requireMention: false } } } } }",
  "baseHash": "<hash>"
}'
```

## 설정 포함
설정을 여러 파일로 분리:

```json5
{
  gateway: { port: 18789 },
  agents: { $include: "./agents.json5" },
  broadcast: { $include: ["./client1.json5", "./client2.json5"] }
}
```
